The "marketing_campaign_data.csv" is the input dataset on which we performed the data preprocessing for analysis and understanding of data.

The "marketing_customers_cleaned.csv" is the output datset which has cleaned data without missing values or null values, extreme values, outliers, etc.
